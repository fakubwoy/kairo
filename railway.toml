[build]
builder = "NIXPACKS"

[deploy]
startCommand = "/opt/venv/bin/gunicorn app:app --bind 0.0.0.0:8080 --workers 1 --threads 4 --timeout 120 --preload"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"